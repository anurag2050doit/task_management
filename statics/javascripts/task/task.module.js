(function () {
    'use strict';

    angular
        .module('site.task', [
            'site.task.controllers',
            'site.task.services'
        ]);

    angular
        .module('site.task.controllers', []);

    angular
        .module('site.task.services', []);

})();